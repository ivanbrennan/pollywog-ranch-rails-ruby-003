<h1>Create a new tadpole for <%= @frog.name %></h1>

<%= form_for Tadpole.new do |f| %>
  <%= f.label :name %>: <%= f.text_field :name %><br>
  <%= f.hidden_field :color, :value => @frog.color %>
  <%= f.hidden_field :frog_id, :value => @frog.id %>

  <%= f.submit "create" %>
<% end %>

<%= link_to("Cancel", tadpoles_path) %>
